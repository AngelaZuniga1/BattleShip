package com.example.battleship.patterns;

// Observer.java - Observer interface

/**
 * Observer interface for the Observer pattern.
 * Allows objects to observe changes in game state.
 */
public interface Observer {
    void update();
}
